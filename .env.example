PORT=3000
JWT_SECRET=your-super-secret-key-change-this-in-production
# If you deploy to a domain, update CORS settings if strictly needed, though current setup is open for simplicity with static frontend
